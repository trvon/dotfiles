# Pi Harness — migrated

This directory previously held a full custom Pi hareness dotfiles setup (extensions, tests, model routing, compaction, watchdog, research orchestrator, RLM integration, and trace tooling).

## What replaced it

| Component | Replacement |
|-----------|-------------|
| Pi loop / task management | [`@trevonistrevon/pi-loop@0.1.3`](https://www.npmjs.com/package/@trevonistrevon/pi-loop/v/0.1.3) |
| Pi task extensions / slash commands | [`tintinweb/pi-tasks`](https://github.com/tintinweb/pi-tasks) |

## Archive

All previous dotfiles (extensions, tests, models.json, settings.json, health-watchdog, hybrid-optimizer, semantic-compressor, compaction-guard, research-orchestrator, runtime-trace, stream-saver, ultrawork harness, MLX wrapper, and backups) are preserved in:

```
./archive/
```

See `./archive/README.md` for the original documentation and setup instructions.
